---
import type { CollectionEntry } from 'astro:content';
import WorkCard from './WorkCard.astro';

interface Props {
  title: string;
  entries: CollectionEntry<'work'>[];
  animated?: boolean;
  indexOffset?: number;
}

const { title, entries, animated = true, indexOffset = 0 } = Astro.props;
---

<section class="mb-12">
  <h2 class="animate-fade-up font-mono text-11 font-normal text-faint tracking-caps uppercase m-0 mb-5 pb-2 border-b border-line" style="--delay:100">
    {title}
  </h2>
  <div class="grid grid-cols-1 gap-4 sm:[grid-template-columns:repeat(auto-fill,minmax(300px,1fr))]">
    {entries.map((entry, i) => (
      <WorkCard
        entry={entry}
        animated={animated}
        index={indexOffset + i}
      />
    ))}
  </div>
</section>
